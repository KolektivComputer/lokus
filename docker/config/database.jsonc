{
  // Used when running the API via docker compose (profile: full).
  // Host "postgres" is the compose service name.
  "jdbcUrl": "jdbc:postgresql://postgres:5432/locus",
  "driver": "PostgreSQL",
  "username": "locus",
  "password": "locus"
}
