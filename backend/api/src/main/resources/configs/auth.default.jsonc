{
  // Copied to CONFIG_ROOT/auth.jsonc on first boot if missing.
  // Change jwt.secret before any real deployment.
  "argon": {
    "saltRounds": 10
  },
  "jwt": {
    "secret": "change-me",
    "issuer": "locus",
    "audience": "locus",
    "realm": "locus"
  }
}
