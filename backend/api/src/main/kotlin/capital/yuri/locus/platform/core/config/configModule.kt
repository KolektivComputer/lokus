package capital.yuri.locus.platform.core.config

// Config is bound in capital.yuri.locus.platform.core.appModule — this file kept
// only so old imports fail loudly at compile time if anything still references
// the previous createConfigModule / configModule API.
